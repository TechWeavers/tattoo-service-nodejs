<div class="main-panel ">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                
                    <div class="row col-sm">
                        <div id="ordem-ativo" class="col-auto pasta-ordem">
                            <a href="/listar-usuarios">Lista de Usuario</a>
                        </div>
                        <div id="ordem" class="col-auto  pasta-ordem">
                            <a href="/novo-usuario">Cadastrar Novo Usuario</a>
                        </div>
                    </div>
                
                <div class="card-body card-pasta bg-orange">

                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID Colaborador</th>
                                    <th>Usuário</th>
                                    <th>Senha</th>
                                </tr>
                            </thead>
                            {{#each usuarios}} {{#dataValues}}
                            <tbody>
                                <tr>
                                    <td class="py-1">{{fk_colaborador}}</td>
                                    <td>{{usuario}}</td>
                                    <td>*****</td>
                                    <td>
                                        <button class="btn btn-secondary"><a href="/editar-usuario/{{id_usuario}}"
                                                style="color:inherit">Edit</a></button>
                                        <button type="button" class="btn btn-primary" id="confirmDelete"><a
                                                href="/excluir-usuario/{{id_usuario}}"
                                                style="color:inherit">Delete</a></button>
                                    </td>
                                </tr>
                            </tbody>
                            {{/dataValues}}
                            {{/each}}
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
   